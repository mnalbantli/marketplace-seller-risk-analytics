# STATUS

Tracks the 8 Definition of Done items from [CLAUDE.md](CLAUDE.md). Updated by Claude Code as build progresses.

| # | Item | Status | Notes |
|---|---|---|---|
| 1 | dbt project on Databricks: staging → intermediate → tested marts for seller/funnel/GMV data | In Progress | Five staging models built and tested: `stg_sellers`, `stg_orders`, `stg_products`, `stg_category_translation`, `stg_order_items` — all in `models/staging/`, all passing (`dbt test`, 2026-08-23, 7/7 PASS on the latest on top of the earlier 10/10). `stg_products` and `stg_category_translation` rename `product_category_name`→`category_name` (and `_english` variant) for a `USING(category_name)` join. `product_category_translation` is a `dbt seed` from the real Olist CSV, referenced via `ref()` not `source()`. `stg_order_items` — the orders↔sellers join point and GMV source (`price`, `freight_value`) — filters `WHERE order_id IS NOT NULL` to drop ~32,951 join/union-artifact rows found upstream in the raw table, drops `shipping_limit_date` and 8 dead product-attribute columns (all NULL on real rows), and its `(order_id, order_item_id)` grain is checked with a singular test (`tests/assert_stg_order_items_unique_grain.sql`, no `dbt_utils` dependency added). `models/example/` scaffold removed; `dbt_project.yml` cleaned up. All of the above is **committed and pushed** to `origin/master` (commit `3e062b4`, on top of `8e91ff4` → `0ea8f9d` → `c737d26` → `36e6910` → `b3f47de`). **Databricks workspace migration (2026-08-24): data re-uploaded to a new workspace; `dbt run` + `dbt test` re-verified against it for all four core staging models — `stg_sellers` (1/1 PASS), `stg_orders` (3/3 PASS), `stg_order_items` (8/8 PASS), `stg_products` (2/2 PASS) — 4/4 models built, 14/14 tests green. No model logic changed; only the underlying connection/workspace.** **`intermediate/` now has its first model (2026-08-24): `int_seller_order_activity` — one row per `seller_id`, joining `stg_orders` to `stg_order_items` on `order_id`. Full spec candidate-directed (grain, bucketing, rate/recency/trend-window definitions all locked by candidate; Claude Code implemented to spec, did not originate logic). Reference date = `MAX(order_purchase_timestamp)` across the dataset, computed once. `order_status` bucketed into `delivered` / `cancelled_unavailable` (canceled, unavailable) / `unresolved_unknown` (everything else). `gmv` and `order_count` are delivered-only. `cancellation_rate`/`unresolved_rate` are each bucket's distinct order count over total distinct order count. `recency_days` = reference date minus the seller's last delivered order's `order_purchase_timestamp` (order-placed basis, not delivery-arrived basis — corrected mid-build from an initial `order_delivered_customer_date` draft). `trend_eligible` = delivered `order_count >= 4`; `orders_last_30d`/`orders_prior_30d` are two contiguous, non-overlapping 30-day delivered-order-count windows immediately before the reference date, NULL unless `trend_eligible`. Tested: `unique`+`not_null` on `seller_id`, `not_null` on `gmv`/`order_count`, plus a singular test (`tests/assert_int_seller_order_activity_rate_bounds.sql`, no `dbt_utils` dependency, same convention as the staging-layer singular test) asserting both rates fall in `[0,1]` — 5/5 PASS. Committed as `5d886e5` on top of `03ff2d2`, pushed.** **`int_seller_category_mix` (2026-08-24): one row per `(seller_id, category_name)`, joining `stg_order_items` to `stg_products` and `stg_orders`, delivered-only, NULL-category items excluded entirely (~1.3% of marketplace GMV, see PROCESS.md). `category_gmv` = `SUM(price)`; `category_gmv_pct` divides by the seller's own summed total across their known-category rows (per-seller denominator). Does not collapse to one row per seller — that's the mart layer's job. Sellers with only NULL-category sales are simply absent, no stub row. Tested: `not_null` on all 4 columns, plus singular tests for `(seller_id, category_name)` grain uniqueness and `[0,1]` bounds on `category_gmv_pct` — 6/6 PASS. Committed as `06335f3`, pushed.** **`int_seller_acquisition` (2026-08-25): built `stg_qualified_leads` and `stg_closed_deals` staging models first (thin pass-throughs), then joined them on `mql_id`. One row per `seller_id`, only for the 842 sellers present in `stg_closed_deals` — no stub rows. Key finding, not in the original spec: only 380 of those 842 `seller_id`s actually exist in `stg_sellers` (real transacting sellers) — the other 462 have no order data anywhere else in the project. All 842 rows kept regardless (no inner join to `stg_sellers` at this layer, per candidate decision) — mismatch surfaces naturally wherever this joins to the transacting-seller universe. This corrects PROCESS.md's acquisition-channel coverage figure from ~27% (842/3,095) to ~12% against the real transacting population (380/3,095) — candidate is updating PROCESS.md separately. `origin` kept as NULL where present (4 of the 380 real-seller rows), not excluded or imputed. Tested: `unique`+`not_null` on `seller_id` (both staging tables and the intermediate model) — 8/8 PASS. Committed as `19c4797`, pushed.** **`marts/` now has its first model (2026-08-25): `mart_seller_health` — the feature table for the future churn-risk model (item 3/4 below), does NOT yet include a risk score or GMV-at-risk. One row per `seller_id`, base is `int_seller_order_activity` (population confirmed identical to `stg_sellers`, 3,095 sellers, no zero-order population exists). Left-joined to `int_seller_category_mix` collapsed to one row per seller via `ROW_NUMBER()` for `top_category`/`category_concentration_pct` (ties broken alphabetically by `category_name` — 3 sellers had an exact tie as of 2026-08-25; tie-break is deterministic only, not business-meaningful; `category_concentration_pct` carried through as-is from `category_gmv_pct`, not recomputed); NULL for both if the seller has no known-category sales. Left-joined to `int_seller_acquisition` for `origin` (NULL for ~88% with no funnel match) and to `stg_sellers` for `seller_city`/`seller_state`. Tested: `unique`+`not_null` on `seller_id`, `not_null` on `gmv`/`order_count` — 4/4 PASS. Committed as `9954b0f` on top of `19c4797`; **not yet pushed** — this shell still has no GitHub auth configured (`git push` fails with "could not read Username for 'https://github.com'"), candidate needs to push manually.** |
| 2 | GitHub Actions CI running `dbt test` on push, demonstrably fails on a broken PR | Not started | |
| 3 | Logistic regression churn-risk model + segment-driver interpretation layer, correlational language only | Not started | |
| 4 | GMV-at-risk quantification joined to risk segments | Not started | |
| 5 | One Tableau dashboard: who's at risk, GMV exposure, distinguishing characteristics, prioritization view | Not started | |
| 6 | Documentation: README + case-study page (Olist-saturation risk, B2B reframe, funnel-attribution limitation, associative-not-causal framing) | Not started | |
| 7 | (Stretch, optional) Temporal validation split | Not started | |
| 8 | (Stretch, optional) Minimum seller-activity threshold + churn-window sensitivity check | Not started | |
